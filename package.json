{
  "name": "vibecheck",
  "version": "1.0.0",
  "description": "Training app with intentionally broken UI and like business logic.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
